---
title: "List Subscriptions"
openapi: get /subscriptions
---

# Guide to Query Params
The `GET /subscriptions` endpoint returns both V1 and V2 subscriptions when called without any parameters. The endpoint supports the following query parameters:
- The **version** query parameter allows you to filter subscriptions by version. The possible versions are `v1` and `v2`.